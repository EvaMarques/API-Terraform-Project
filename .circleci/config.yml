version: 2
jobs:
  deploy:
    docker:
      - image: hashicorp/terraform:full
    steps:
      - checkout
      - run: terraform init
      - run: which zip
workflows:
  version: 2
  build-deploy:
    jobs:
      - deploy
